@model IEnumerable<EnterpriseManagementApp.Models.Rentals.LeaseAgreement>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RenterID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AssetID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RentAmount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RenterID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AssetID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RentAmount)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LeaseID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LeaseID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LeaseID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
