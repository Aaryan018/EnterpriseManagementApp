@*@model IEnumerable<EnterpriseManagementApp.Models.Rentals.Renter>

@{
    ViewData["Title"] = "Renters";
}

<h1>Renters Management</h1>

<div class="mb-4">
    <a asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus-circle"></i> Add New Renter
    </a>
</div>

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead class="thead-dark">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.FirstOrDefault()!.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FirstOrDefault()!.Address)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FirstOrDefault()!.EmergencyContact)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FirstOrDefault()!.OccupancyStartDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.FirstOrDefault()!.OccupancyEndDate)
                </th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Address)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EmergencyContact)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.OccupancyStartDate, "{0:dd MMM yyyy}")
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.OccupancyEndDate, "{0:dd MMM yyyy}")
                    </td>
                    <td>
                        <div class="btn-group" role="group">
                            <a asp-action="Edit" asp-route-id="@item.RenterID" class="btn btn-sm btn-warning">
                                <i class="fas fa-edit"></i> Edit
                            </a>
                            <a asp-action="Details" asp-route-id="@item.RenterID" class="btn btn-sm btn-info">
                                <i class="fas fa-info-circle"></i> Details
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.RenterID" class="btn btn-sm btn-danger">
                                <i class="fas fa-trash-alt"></i> Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}*@