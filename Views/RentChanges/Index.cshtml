@model IEnumerable<EnterpriseManagementApp.Models.RentChange>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Asset)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ChangeDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OldRate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Asset.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ChangeDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OldRate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RentChangeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RentChangeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RentChangeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
